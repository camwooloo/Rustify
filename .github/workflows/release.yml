# Builds Rustify for Windows, macOS and Linux and attaches one download per
# platform to the release for the tag.
#
# The app ships the daemon and the CLI alongside it as bundle resources, so
# each job builds those first and drops them where the bundler expects, under
# the name that platform uses.

name: Release

on:
  push:
    tags: ["v*"]
  # So the build can be exercised without cutting a release.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Nothing is published without the suite passing. Linux alone: the tests
  # cover parsers and pure logic, none of which is platform-specific.
  test:
    name: Tests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libasound2-dev libssl-dev
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"
          key: tests
      # Tauri's build script checks that every bundle resource exists before
      # it will compile the app crate at all, and the daemon is one of them.
      # The tests never read these files — they only have to be there — so
      # empty stand-ins buy the app crate's tests without building librespot
      # twice on the way to running them.
      - name: Stand in for the bundled binaries
        run: |
          mkdir -p app/src-tauri/binaries
          touch app/src-tauri/binaries/rustifyd app/src-tauri/binaries/rustify-ctl

      # The ignored ones reach the live stats.fm and GitHub APIs, so they are
      # left to run by hand rather than made a condition of releasing.
      - run: cargo test --workspace

  build:
    needs: test
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      # One platform failing should not hide whether the others work.
      fail-fast: false
      matrix:
        include:
          - name: Windows
            os: windows-latest
            bundle: nsis
            pattern: "*-setup.exe"
          - name: macOS
            os: macos-latest
            bundle: dmg
            pattern: "*.dmg"
          - name: Linux
            os: ubuntu-22.04
            bundle: appimage
            pattern: "*.AppImage"

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          # webkit2gtk and friends build the window; alsa builds the player.
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libasound2-dev \
            libssl-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"
          key: ${{ matrix.os }}

      - name: Build the daemon and the CLI
        run: cargo build --release -p spotify-daemon

      - name: Stage them for the bundler
        shell: bash
        run: |
          mkdir -p app/src-tauri/binaries
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp target/release/rustifyd.exe app/src-tauri/binaries/
            cp target/release/rustify-ctl.exe app/src-tauri/binaries/
          else
            cp target/release/rustifyd app/src-tauri/binaries/
            cp target/release/rustify-ctl app/src-tauri/binaries/
            chmod +x app/src-tauri/binaries/rustifyd app/src-tauri/binaries/rustify-ctl
          fi

      - name: Build the app
        run: cargo install tauri-cli --version "^2" --locked && cargo tauri build
        working-directory: app/src-tauri

      - name: Collect the download
        shell: bash
        run: |
          mkdir -p dist
          find target/release/bundle -name "${{ matrix.pattern }}" -type f -exec cp {} dist/ \;
          ls -la dist

      - uses: actions/upload-artifact@v4
        with:
          name: rustify-${{ matrix.name }}
          path: dist/*

      - name: Attach it to the release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          fail_on_unmatched_files: true
